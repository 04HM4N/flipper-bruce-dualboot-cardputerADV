# Name,   Type, SubType, Offset,    Size,      Flags
# -----------------------------------------------------------------------------
# Bruce-side view of the 8 MB dual-boot layout: identical to
# partitions_dualboot_8mb.csv plus a "spiffs" (LittleFS) partition, because
# Bruce hardcodes the partition label "spiffs" for its internal filesystem
# (src/core/sd_functions.cpp: LittleFS.begin(false, "/littlefs", maxFiles)).
# The Flipper port does not use this entry (its own table omits it).
#
#   ota_0   = ElicoftZ Flipper Zero port (default boot target, 3.0 MB)
#   ota_1   = Bruce firmware             (3.9375 MB)
#   spiffs  = Bruce internal LittleFS    (bruceConfig, U2F creds, templates...)
#   storage = FAT data partition (shared; used by Flipper as its /ext)
# -----------------------------------------------------------------------------
nvs,        data, nvs,      0x9000,    0x4000,
otadata,    data, ota,      0xd000,    0x2000,
phy_init,   data, phy,      0xf000,    0x1000,
ota_0,      app,  ota_0,    0x20000,   0x300000,
ota_1,      app,  ota_1,    0x320000,  0x3f0000,
spiffs,     data, spiffs,   0x710000,  0xc0000,
storage,    data, fat,      0x7d0000,  0x30000,
